see files:
   hparams.json  for best fitting model hyper-params
   train_val_curves.pdf for the train, val curves.
   train_metrics.jso for the train, val metrics
   test_metrics.json for the test metrics

HPO objective was validation dataset composite NDCG@20.  
   best: 0.27

The training, validation curve logits are stable and
reasonable in magnitude.  Stable learning is seen
in all of the metrics.  15 epochs looks like enough
training avoiding overfitting (memorization
of the dataset).

test dataset metrics:
"test_composite_ndcg_20": 0.26536538277534727, 
"test_ndcg_20": 0.39800726796718355,
"test_ndcg_head_20": 0.5563947393539104,
"test_ndcg_tail_20": 0.07414617448886658,
"test_ndcg_torso_20": 0.20261356646710255,
"test_mrr_20": 0.28624633430166446,
"test_precision_1": 0.15606299867021275,
"test_precision_20": 0.039541434734425646,
"test_precision_5": 0.08592503723946024,
"test_recall_20": 0.7908286236702128,
"test_recall_head_20": 0.9543766696402367,
"test_recall_tail_20": 0.25315330026948707,
"test_recall_torso_20": 0.6004341726607465}
"test_loss": 3.3560463063260344,

see file opt_gpu_notes.txt for the gradual improvements in reducing runtime.
the file is a work in progress.


editing ...

Then final test metrics performed by sampling randomly num_candidates for each
user in the test dataset of all ratings.  n_draws are done for each user.
The statics for NDCG@20, MRR@20, RECALL@20, PRECISION@20, and F1SCORE@20 
were calculated and then group stats calculated:
== Evaluation Results (K=20) ===
Metric       | Mean     | Median   | MAD      | MAD-Std 
--------------------------------------------------------
NDCG         | 0.6857   | 0.6912   | 0.1242   | 0.1842
MRR          | 0.8465   | 0.9500   | 0.0500   | 0.0741
Recall       | 0.4768   | 0.4666   | 0.0457   | 0.0678
Precision    | 0.6459   | 0.6600   | 0.1400   | 0.2076
F1-Score     | 0.5273   | 0.5485   | 0.0463   | 0.0687

Additionally, highly polarized viewers were chosen who primarily loved
movies from one genre and had significant number of ratings in both
(train+val) and test.
  recommendations for those users were made, excluding already seen,
  and then the results were examined to see whether they were reasonable:
  <in progress ...>


--------
Note that in training a batch size of 256, and choosing k=20 out of them and
the batch of N items having only 1 positive (==relevant item) and the rest
being negative, we can estimate NDCG@20 for randomly choosing any of the N:
  random, batch_size = 256:
    prob of the single positive landing at any rank: (1/N)

    E[NDCG@20] = (1/N) * sum_{i=1,k}( 1/log2(i+1) )
               = 0.0275

  random, num_candidates = 70:
    E[NDCG@20] = (1/N) * sum_{i=1,k}( 1/log2(i+1) )
               = 0.101

  best:
    always has the single relevant at position 1:
    
    E[NDCG@20] = 1.0


For inference, we have top_k items and all are relevant and have different ranks.  
We want to calculate what NDCG@top_k would be for random ordering of the list.

   random: 
       each item has a probabaility of (1/top_k) of landing at any rank i.

       mu_20 = the avg relevance score of the top_k=20 items = (1/top_k) * sum_{k=1,top_k}(rel_k}
           = (1/top_k) * arithmetics series for adding 1 to 1 through top_k
           = (1/top_k) * (a_0*n + (n*(n-1)/2)*d) where a_0=1, n=top_k, d=1
           = (1/20.) * 210.
           = 10.5

       the total discount factor = (sum_{i=1,top_k}( 1/log2(i+1) )
           = 7.04

       for a random shuffle, every position is expected to have the mean relevant mu_20.
       E[NDCG@20] = mu_20 * tot_discount
                  = 10.5 * 7.04
                  = 73.92

       the ideal DCG@20, if no ground truth is known, must be estimated.
         options:
            (1) assume the top_k=20 are all relevant, then
                  score them to rank them using one of the following:
                  human labels,
                  user clicks,
                  LLM judged relevance grades.
            (2) give the top_k retrieved to an LLM for scoring.
            (3) use binary scores = 1 user clicks and user purchases,
              

