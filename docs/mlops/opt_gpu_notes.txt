running on kaggle and trying to increase the use of GPUs
to decrease the overall runtime.

kaggle doesn't have Direct P2P nore NVLink/PCIe so GPU 
communication between one another on the T4x2 uses shared 
memory SHM.

For my iniital test of times on laptop, then Kaggle, I
used the elapsed time for a batch, and scaled those time
for estimates.  The problem with that approach is that 
the time estimates don't include dataloading backlog.
A better approach is to bootstrap from start to finish of
an HPO tune phas of 1 trial, but using num_epochs >= 4 to
get a more realistic use of memory.

Here are details of the first tune trial on Kaggle, showing
that the grain configuration I used may be using too many 
threads, leading to thread thrashing.  The GPUs are mostly
waiting for data.

kaggle times:
   batch _size 256, 473,000 train, 70,000 val
   10 epochs, 1 trial
   took 9.6 hours

   100 batches took 5 sec => 0.05 sec/batch
   0.05 sec/batch * (463000/256) batches  = 90 sec per epoch

epoch 0 : 1 = 36+16 = 52 min
      1 : 2 = 29+14 = 43 min
      2 : 3 = 25+31 = 56 min
      3 : 4 = 22+35 = 57 min
      4 : 5 = 22+38 = 60 min
      5 : 6 = 21+38 = 59 min
      6 : 7 = 27+39 = 66 min
      7 : 8 = 25+33 = 58 min
      8 : 9 = 22+35 = 57 min

NEXT attempt with a single train phase will use:
    grain_worker_count=2 \
    grain_read_options_num_threads=4 \
    grain_read_buffer_size=10 \
where before I had used 6, 8, and 50, respectively.
The long epoch runtimes might be from thread thrashing.

Note that the "elapsed time" in the logs is best to use for estimating times
for other runs rather than the batch to batch times.

Before running again on Kaggle,
  - have added prefetching put onto GPU of grain dataloader super padded graphs.
  - have added CPU logging.
  - have switched to faster gpu stats for logging.

Update after running on Kaggle with those changes:
   - the prefetching onto GPU helped tremendously, a factor of about 24
     10 epochs of training plus validation at each epoch took 25 minutes for the 463,000 sample dataset.
     1 epoch = 2.5 min

a pipeline run of 10 HPO trials with num_epochs=15, followed by train for 15 epochs and test
    took 3 hrs, 45 min

That prefetching of buffer size=2 used python threads, which produced a deadlock after the
first lucky successful pipeline run on Kaggle.
python thread GIL deadlocked with C++ XLA client's strict locking rules regarding the CUDA context.

The fix:
I changed the prefetching to a python generator with a queue, no python threads.
The result is about 3 times slower epoch runtime unfortunately due to sequential
loading even though jax device put is asynchronous.
   previously, the elapsed time for 15 epochs was about 1100 sec.
      => 73 sec per epoch
   now, the elapsed time for 20 epochs is about 7180 sec.
      => 359 sec per epoch
         so can only run 6 trials of 20 epochs in 12 hours now.

TODO: consider options to bring the runtime back down.
 - only the main python thread should touch the XLA, JAX, or CUDA contexts.
 - one approach is to use rust in between grain dataloader and the loading onto GPU.
     in rust: release the python GIL using py.allow_threads(|| {...}) then
     construct the padded super graph.  use pyO3 rust-numpy or DLPack protocol.
     the memory allocated by rust is in C-contiguous format and passed as
     a raw pointer back to python.  jax supports dlpack so can ingest the memory buffer
     with zero-copy overhead.
     the python main thread uses jax.device_put() to schedule the PCIe non-blocking
     transfer.
     To maintain a buffer of 2 batches actively queued for the GPU without using
     any background Python threads, your main training loop simply needs to
     maintain a rotating queue of rust futures.
     gpu_batch_curr = jax.device_put(cpu_batch_0)
     gpu_batch_next = jax.device_put(cpu_batch_1)
   worth the savings in GPU time for a product whose train pipeline
   will be used over long period of time in CI/CD pipeline...
 - another approach is that  C++ component could replace the super padded graph assembly in the
       background, freeing up the python main thread to focus on the
       GPU transfer queue.
     - could use pybind11 to build a C++ python extension and
       use DLPack to pass memory arrays from C++ to JAX w/o copying or serialization.
     - on python main thread grain handles reading data, shuffling etc
       yielding numpu arrays
     - the custom C++ Extension for background graph assembly
       is exposed to python via Pybind11.
       - it immed uses py::gil_scoped_release.
       - now python is unlocked an movied to next task, so this
         extension can use std::thread pools to form the final
         dense graph of raw C++ memory buffers (std::vector<float>) etc
         then the assembled buffers are pushed into a standared
         C++ queu protected by a mutex.
     - python training loop calls .get_next_batch() on the C++ object
       which passes the memory pointer via a DLPack PyCapsule.
     - python weaps it into a jax array batch
       and uses jax.device_put or similar to load onto GPU
     - python invokes c++ delete of relevant memory after eval_step




