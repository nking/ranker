goal is to improve personalization in movie recommendations 
for the user.

this will be measured by NDCG@20 metric.

This is often accomplished by following the bi-encoder
+ Retrieval with a cross-encoder.

The cross-encoder (Ranker) in this project will use
a graph attention network with efficient, query-dependent
attention, a.k.a. GATv2, as the first layer in a
neural network, followed by a linear layer for scoring. 

GraphRanker as graph contextualized listwise ranker:
    GNN Cross Encoder (JraphX):
        Processes the local graph (user + candidates) to capture
        structural relationships (e.g., "movie_a is similar to movies user liked").
        jraphx has support for GATv2
    Listwise Interaction (Flax NNX):
        A small Transformer block that "compares" all candidates
        in the list simultaneously.
    Ranking Loss with (Rax)


The data needed by the model are a little complex so are
listed in detail here.
Data needed:
  - user and movie embeddings
      - created as outputs of bi-encoder in rec sys/tfx pipelines project
        - formats are array_record
    - user recommendations: ANN search results of movie_ids of movie vectors
          ranked by highest similarity to uer vector
      - created as outputs of bi-encoder in rec sys/tfx pipelines project
        - formats are array_record
    - ratings
      - created rec sys/tfx pipelines project
        - formats are array_record
    - movies catalog
      - created rec sys/tfx pipelines project
        - formats are array_record
    - in production:
       perform the ETL above using ApacheBeam/Spark (possibly via Dataflow)
       with BigQuery and Google Cloud Storage as target destinations.
       - track data lineage with Knowledge Catalog (formerly Dataplex Universal
         and Data Catalog)
To be implemented in this project:
- the GraphRanker model
- MLOps pipelines for all stages (details are below)

Model requirements:
  GraphRanker
  - must be trainable on tpu, gpu, and cpu
  - must be highly scalable
  - must be highly distributable
  - must run on linux OS at least
  - inference have fast response times (on the order of msec)
  - deployment should be highly available for online inference and
    have low latency.
  - must be capable of inference and bulk inference
    - given:
      a query of user ...<TBD>, unranked recommended movies
      returns ranked list of movies
  - trained model eval metrics ndcg@k should be an
    improvement over the un-ranked retrieval results
    and should be at least <TBD>.
  - model, data, and state echeckpointing
  - experiment tracking that works on local cpu or can be
    used to store into cloud specific solutions like
    Vertex AI, SageMaker, Azure ML, etc

I will use the Jax AI stack for creating the model
    Jraphx + Flax NNX + Rax
    and will save it to a JAX serving format and/or __

Note: I worked through using ray[tune,train] for part of
the MLOps, but it is in a state of transitioning to v2 and
the Tuner is not compatible with the new v2 trainer, just one
major bug, but after 1 day of trying to configure it, will
try another framework, that is more stable and or simpler 
if possible.
options:
- google's vizier for hypertune training
   - version compat conflicts only with xmanager.
- vizier, optuna, katib


Dataloading:  Google Grain
State Managements: Orbax
Experiment Tracking: MLFlow-skinny
MLOps workflow tasks:  
code container: Docker or equiv
orchestrator: xmanager for dev, kfp for dev and production

MLOps details:
  tasks for the workflow or indiv tasks:
  the stars are highest priority
  - training and deployment pipeline:
    - * ingest data
        - pre-processing to create input array_reord format
          files has been done with apache beam in the 
          retrieval project
        - Grain is used in this project to create data loader
    - calc statistics of data
    - generate data schema and perist it
    - validate data statistics
    - compare data to latest artifact
    - * transform data
    - * tune the model for best hyper-parameters
    - * train a model with best hyper-params
    - * evaluate model performance from the validation data
      metrics compared to baseline model.
      result is "BLESSED" or "NOT_BLESSED"
    - allow for approval
  - deployment pipeline
    - BLESSED and approved model can be deployed to serving
  - test pipeline:
    - <TBD> aside from metrics
  - monitoring pipeline:
    - <TBD>

mlflow-skinny has 
   Tracking API (log_metric, log_param)
   External Plugins (GCP/Vertex, AWS, Azure)

orbax checkpoint has
   ability to store to local disk, GCS, AWS S3, Azure storage blob

mflow and vizier need a multithread capable sql database
orbax and the input data files need an object store

-------------

consider ways to reduce the runtime execution
- if had enough RAM, could change the _epoch_validation back to a vectorized
  operation instead of chunked.

- different architectures:
  - the re-ranker fine-tunes a LiT5 reranker
