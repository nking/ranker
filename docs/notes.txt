goal is to make a ranker for the recommender systems
and MLOps pipelines for it.

In choosing data loading framework:
(1) Google's grain from the Jax AI Stack.

In choosing the LTR model, there are 3 basic approaches:
(1) gradient boosting
    - good for tabular data, very fast, very accurate
    - cannot handle OOV, that is, cold starts
    - lightGBM, use objective lambdarank
    - xgboost, use objective rank:ndcg or rank:pairwise
    - catboost, use loss function yetirank or pairlogit
    - lightgbm and xgboost have Dask integrations for distributed training
    - MMLSpark for lightgbm for distr training
    - ray for distributed training
(2) DNN listwise ranker
  - TF-Ranking is deprecated
  - Rax (JAx based ranking) is active
(3) graph based listewise ranker
    Graph Transformer (GAT) as graph contextualized listwise ranker:
  - Jraphx + Flax NNX + Rax
    - first:
      GAT2 GNN Encoder (JraphX): 
      Processes the local graph (user + candidates) to capture 
      structural relationships (e.g., "This movie is similar to 3 others you liked").
    - then:
      Listwise Interaction (Flax NNX): 
      A small Transformer block that "compares" all candidates 
      in the list simultaneously.
    - then Ranking Loss (Rax): 
      Optimizes the entire list order using a listwise objective 
      like rax.softmax_loss.
    - handles OOV, is fully inductive

Test, model evaluation Metrics:
- listwise ranking metrics
  NDCG@k, precision@k, recall@k, f1sore@k, MRR@k

hyper-parameter tuning options:
- OSS Vizier
- Optuna
- Ray Tune
- katib for use with kfp
summary: below, version conflicts are mentioned
leaving optuna and katib as best options.
katib uses optuna internally.

In choosing the model serialization and serving:
- Orbax for jax checkpointing and serialization
- jax serving format
- consider Neptune Serving Engine compatibility

In choosing an MLOps pipeline and ML APIs:
- ML APIs need to be compatible with Jax AI Stack, cloud compatible
  and agnostic where possible.
- MLOps needs data parallel distributed training.
  that means jax aware and jax using in order to be
  able to use grain's data sharding.
- ML APIs and MLOps need experiment tracking and logging details such
  as metrics.  needs visualization.
- orchestrator and MLOps in general
  might need to exist in venv separate from the
  jax ai stack
  - Jax AI stack has jax==0.10.0, which for python 3.12
     has fixed protobuf==7.34.1, but so far all seems well
     when protobuf==6.33.6
- KFP requires protobuf>=6.31.1,<7.0
- xmanager==0.7.1 requires sqlalchemy==1.2.19, but
  only the very first tag google-vizier==v0.0.2-alpha
  SQLAlchemy==1.2, all else have >= 1.4,
  SQLAlchemy doesn't officially support threading and
  memory management of py3.12, so needs py3.11
- google-vizier==0.1.24 installs  SQL 2.0.49
  so cannot share same venv as xmanager,
  but can co-habitate w/ the jax ai stack.
==> an orchestator env w/ google-vizier==v0.0.2-alpha
    and a seperate training container env using
    google-vizier==0.1.24 have schema differences
    and gRPC protocol differences.
- optuna==4.8.0 says sqlalchemy>=1.4.2
  need to use pip install optuna -c orchestrator_constrants.txt
  to keep it from upgrading SQLAlchemy
- optuna as a tuner does not have the version conflicts
  - in the orchestrator environment:
     - use optuna to init a study and define search space
  - in the training container:
     - use optuna to get study suggested params w/ study.ask()
       and report the results using study.tell()
  - can use postgres, mysql,
    - or a smaller footprint db?
  - with some coding for it, should be easy to reuse same
    code when xmanager + optuna are orchestration
    and when kfp + katib are orchestration.
- most common options that work in local and cloud environments,
  - KFP + Kubeflow Trainer
    Kubeflow Trainer injects jax environment into each container
    - JAX_NUM_PROCESSES: The total number of pods/containers in your distributed run.
    - JAX_PROCESS_ID: The unique rank of that specific container.
    - JAX_COORDINATOR_ADDRESS: The IP of the master node (rank 0).
    KFP launches a new TrainJob for each trial

  that don't have version conflicts mentioned above.
  - KFP as the orchestrator running in its own virtual env
    - docker container images holding the code which needs
      the JAX AI stack libraries
    - katib as tuner, but katib is using optuna
  - KubeRay as a kubernetes operator or ManagedRay
    - docker container images holding the code which needs
      the JAX AI stack libraries
    Ray handles JAX TPU/GPU memory management best, but has
    bugs in incompatiblity of their Tuner with new v2 Train.
  - xmanager as orchestrator
    - optuna as tuner
in summary, am now considering:
  (1) development, tuning, training:
      - xmanager for orchestrator,
        jax ai stack in training container,
        and optuna in both environments
  (2) full mlops, only listing tuning, training here:
      - kfp for orchestrator
        jax ai stack in training container,
        katib for tuning 

Monitoring:
- system performance monitoring:
  - Xprof tensorboard profiler
    - visibilitiy into XLA computation graphs
    - high level optimizer operations
    - memory usage on TPUs and GPUs
    - Conflict of tensorboard w/ python versions > 3.10
      due to setuptools being removed?
  - Perfetto
  - prometheus and Grafana
- ML observability and Drift:
  - Arize Phoenix 
    clusters the embeddings using UMAP or HDBScan 
    to look for drift
    - jax compatible
  - DeepEval or Confident AI
    - runs online evaluations to make sure that a common
      failure like recommending the same 5 movies to all
      users does not occur
  - Alibi Detect
    - online drift detection of attention weights

GAT metrics to monitor outside of the listwise ranking metrics:
- attention entropy
  uniform distribution is not informative, but a dirac delta
  function is too focused
- edge density drift
  - monitoring sudden changes in user to movie interactions
- in-degree distributions
  - monitoring for GAT unable to recommend tail features due
    to a few movies dominating the mode
- node homophily
  - whether similar users are still connecting to similar movies

Note: Gemini was used in searching for ranking architectures
and getting to the graph solution.  Then it was most useful
in providing a good start to efficient implementations of
vectorized ops. For example, how to make efficient masks
that operate on a batch with numpy and alternative ways to
make branchless code.  Useful for all aspects of
quick documentaton answers.

-------
in training model:
- if have memory problems:
  - node count is high:  consider neighbor sampling or cluster-GCN
  - edge count is high: consider edge dropout or subgraph sampling
  - feature count is obtrusively high: consider dim reduction in
                                       pre-processing stage or
                                       bottleneck layers
  - multihead attention taking up large amount of memory:
    consider reducing the number of heads or using checkpointing
    with jax.checkpoint

