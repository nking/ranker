- make chnages to use rust code for data preparation
  taking input ratings and other array_reords 
  to form enriched graph as arrays of fixed size c++ raw memory
  pointers

- consider using a CliffordNet layer instead of the GATv2.
  - requires a change in the bi-encoder to make embeddings
    that are multivectors and replaces the dot product
    with inner and external (a.k.a. wedge) product.
    - the 16 dimension user and movie embedding 
      then becomes 120 in length as a bi-vector (C(16,2))
      -> the addition of the wedge product makes the resulting
         bivector far more expressive, so it might be that the
         geometric algebra bi-encoder should only use n=6 or 7
         instead of 16 to avoid over-fitting.
         -> an n=6 representation bi-vector and a cross-encoder
            built with a geom product layer, lnear layer, and a rotor layer
            would have comparable runtime and space compexity
            as the euclidean gatv2 cross-encoder with d=16 vectors.
            one would expect the GA cross-encoder to be more
            expressive
         -> jax + jaxga or numga would allow efficient use of accelerators.

- add ability to introduce a delay to some of the gradient computations on one worker
  to test that synchronization occurs even when one Pod might be busier than others.

- consider the remaining MLOps components needed:
  - data statistics for raw ingested data and for the features
    - the raw data stats component can use TFDV and pandas
    - the features data would be best handled after the python or c++ code
      below that replaces the grain data loader for inference.
      - then data stats can be used on training or production data features too
  - export model
    - requires tensorflow install for orbax-export to tf savedmodel
    - when add it to the KFP pipeline, consider a PVC for the fakegcs
      server to hold output artifact... not needed in development
      because I'm using mounts and mount paths to local disk
  - monitoring
  - consider NannyML in monitoring

- implement the python or C++ code to replace the grain dataloaders
  for inference using the tf saved model

- consider using kube-linter
   https://github.com/stackrox/kube-linter

- replace use of passwords in the .env and uris with
  a mock service for GCP Secret Manager.
  e.g. can add this to docker-compose-dbs.yaml:

  with services:
  secret-manager-mock:
    image: blackwellsystems/gcp-secret-manager-emulator
    ports:
      - "9090:9090" # gRPC
      - "8080:8080" # REST

  and use it with something like:
    from google.cloud import secretmanager
    import grpc
    if os.getenv("ENV") == "development":
        channel = grpc.insecure_channel("localhost:9090")
        client = secretmanager.SecretManagerServiceClient(
            transport=secretmanager.transports.
                SecretManagerServiceGrpcTransport(channel=channel))
    else:
        client = secretmanager.SecretManagerServiceClient()

    other mock services avail too:
       https://github.com/blackwell-systems?tab=repositories&q=gcp*&type=&language=&sort=name
